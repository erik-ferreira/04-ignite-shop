import { createStitches } from "@stitches/react";

export const {
  css,
  config,
  styled,
  globalCss,
  keyframes,
  getCssText,
  theme,
  createTheme,
} = createStitches({
  theme: {
    colors: {
      rocket: "#8759c1",
    },
  },
});
